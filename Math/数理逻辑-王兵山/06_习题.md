# 习题

1. 用合式公式集的定义证明：如果A和B是合式公式，则\~A和(AVB)也是合式公式.2. 一个公式 $Y$ 的生成序列是指一个有穷的公式序列 $\mathbf{Y}_1, \dots, \mathbf{Y}_m$ ，其中 $\mathbf{Y}_m = \mathbf{Y}$ 且对任意 $i (1 \leqslant i \leqslant m)$ ，下列条件之一成立：

- i) $\mathbf{Y}_i$ 是一个命题变元；
- ii) 有 $j < i$ 使得 $\mathrm{Y_i} = \neg \mathrm{Y_i}$ ;
- iii) 有 $j < i$ 且 $k < i$ 使得 $\mathbf{Y}_i = (\mathbf{Y}_i \vee \mathbf{Y}_k)$ .

证明：Y为合式公式当且仅当Y有一个生成序列。

- 3. 证明: 任何合式公式必为下列三种形式之一: P, $\neg$ A 和 A $\vee$ B, 其中 P 为命题变元, A, B 均为合式公式。
- 4. 利用上题结果, 给出一个确定每个表达式是否为合式公式的有效过程, 并证明它的正确性.
- 5. 下面这个过程用于检测一个复杂的表达式是否为合式公式，或者合式的缩写：给出一个表达式A，从左至右逐个地考虑在A中出现的符号并且从0开始计数，每遇到一个“（”加1，每遇到一个“）”减1.

例如：（ $(\neg \mathrm{r} \vee \mathrm{p}) \vee \neg (((\mathrm{q} \vee \mathrm{r}) \vee (\mathrm{P} \vee \neg \mathrm{S})) \vee \mathrm{q}))$ 12 1 234 3 4 3 2 10证明：若表达式A为合式公式，则最终计数为0，且计数不会出现负值。

6. 确定下列各式是否为合式公式，并证明你的结论。

- a) $\neg \mathbf{P}(\neg \mathbf{q})$
- b) ()P

c) $(\neg \mathbf{PV})\mathbf{q}$d) $\neg (\neg \neg \mathbf{q} \vee \mathbf{r})$7. 设 $\theta$ 是任意一个替换, A 和 B 是合式公式,证明：a) $\theta (\neg \mathbf{A}) = \neg \theta (\mathbf{A})$b) $\theta (\mathbf{A}\lor \mathbf{B}) = \theta (\mathbf{A})\lor \theta (\mathbf{B}).$

- 8. 证明: 若 $\mathfrak{p}_1, \mathfrak{p}_2, \cdots, \mathfrak{p}_n$ 是互不相同的变元, $\mathbf{B}_1, \mathbf{B}_2, \cdots, \mathbf{B}_n$ , $\mathbf{A}$ 是合式公式, 则 $\mathbf{S}_{\mathbf{B}_1, \cdots, \mathbf{B}_n}^{\mathbf{P}_1, \cdots, \mathbf{P}_n}\mathbf{A}$ 是合式公式.
- 9. 若 $\mathbf{C}, \mathbf{D}$ 为命题演算的合式公式, 且 $\mathbf{D} = \mathbf{S}_{\mathbf{q}_1, \dots, \mathbf{q}_n}^{\mathbf{p}_1, \dots, \mathbf{p}_r} \mathbf{C}$ , 证明: 若 $\mathbf{C}$ 是永真式, 则 $\mathbf{D}$ 也是永真式.
- 10. 计算: $\mathbf{S}_{\left(\mathbf{q} \vee r\right), -\mathbf{p}}^{\mathbf{p}}, \mathbf{q} \neg \left(\neg \mathbf{P} \vee \mathbf{q}\right) \vee \neg \neg \mathbf{P})$
- 11. 证明: 若 $\sigma$ 和 $\tau$ 均是替换, 则 $\sigma$ 和 $\tau$ 的合成 $\sigma \cdot \tau$ 也是替换。
- 12. 在任何一个逻辑系统中, 只要以下两条规则:

- i) 若 $\Gamma \vdash \mathbf{A} \lor \mathbf{B}$ , 则 $\Gamma \vdash \mathbf{B} \lor \mathbf{A}$ ;
- ii) 若 $\Gamma\mid-(\mathbf{A} \vee \mathbf{B}) \vee \mathbf{C}$ , 则 $\Gamma\mid-\mathbf{A} \vee (\mathbf{B} \vee \mathbf{C})$

为其原始规则或派生规则，则由 $\mathbf{A} \vee (\mathbf{B} \vee \mathbf{C})$ 就能推出 $(\mathbf{A} \vee \mathbf{B}) \vee \mathbf{C}$13. 设 $\mu$ 是一个与 $\mathcal{P}$ 系统有相同的合式公式和推导规则的系统, 它有唯一的公理模式:

$$
(A \rightarrow B \rightarrow_ {\square} C \vee_ {\square} D \vee E) \rightarrow_ {\square} D \rightarrow A \rightarrow_ {\square} D \vee_ {\square} E \vee A
$$

证明： $\mathcal{S}$ 的每个定理都是 $\mu$ 的定理。

在 $14 \neg 18$ 题中, 确定所给合式公式是否为永真式, 永假式或可满足的, 若为可满足的, 试分别给出为真和为假的指派:14. $(\neg (\mathfrak{p}\wedge \neg \mathfrak{q})\wedge (\neg \mathbf{r}\lor \mathbf{s}))\rightarrow_{\bullet}((s\rightarrow \mathfrak{q})\wedge \neg \mathfrak{p})$

$$
\rightarrow \neg r
$$

- 15. $(\mathbf{r} \vee \mathbf{\Pi} = \mathbf{q} \wedge \neg \mathbf{\Pi} = \mathbf{p} \rightarrow \mathbf{s}) \rightarrow \mathbf{\Pi} = \mathbf{\Pi} \neg (\mathbf{s} \rightarrow \mathbf{r}) \rightarrow \mathbf{\Pi} = \mathbf{\Pi} q \rightarrow p$
- 16. $\neg (\mathbf{p} \vee \mathbf{q}) \equiv \mathbf{\Omega} \neg \mathbf{p} \wedge \neg \mathbf{q}$
- 17. $\mathfrak{p}^{\rightarrow}\mathfrak{q}^{\rightarrow}\neg \mathfrak{q}^{\rightarrow}\neg \mathfrak{p}$

- 18. $\mathbf{p} \wedge (\mathbf{q} \vee \mathbf{r}) \equiv \mathbf{\Phi}(\mathbf{p} \wedge \mathbf{q}) \vee (\mathbf{p} \wedge \mathbf{r})$
- 19. 设 $\mathcal{S}$ 是一个由 $\mathcal{P}$ 增加单个公式 $(\mathfrak{p} \rightarroweq \mathfrak{q})$ 作为公理所得之系统, 证明: $\mathcal{P}$ 是协调的。
- 20. 设 $\mathcal{P}'$ 是一个由 $\mathcal{P}$ 增加一个公理模式 (A $\rightarrow$ B) 所得之系统，试判断 $\mathcal{P}'$ 是协调的吗？为什么？
- 21. 在 $\mathcal{S}$ 系统中是否有不含“ $\vee$ ”的定理？为什么？
- 22. 在 $\mathcal{P}$ 系统中是否有不含“～”的定理？为什么？
- 23. 设 $s$ 为一个只含联结词“～”和“ $\vee$ ”的命题演算系统，且满足：

i) Formula $s =$ Formula;ii) $\mathbf{A}\mathbf{x}\mathbf{i}\mathbf{om} = \{\neg (\mathbf{A}\lor \mathbf{B})\lor (\mathbf{B}\lor \mathbf{C})\mid\mathbf{A},\mathbf{B},\mathbf{C}$ 均为合式公式};iii) Rule $= \{\mathbf{MP}\}$ .证明： $s$ 不是绝对协调的。

24. 设 $\mathcal{P}$ 是一个仅含二元联结词“*”的命题演算系统，且满足：

i) Formula $\alpha$ 归纳定义如下：

- a) 若 $\mathbf{p}$ 为命题变元, 则 $\mathbf{P} \in \mathbf{F o r m u l a}_{\bullet}$ ;
- b) 若 A, B∈Formulaₐ，则(A*B)亦然；
- c) 每个 $A \in \text{Formula}_a$ 都可通过有限次使用 a) 和 b) 获得。

- ii) $\mathbf{A}\mathbf{x}\mathbf{i}\mathbf{o}\mathbf{m} = \{\mathbf{A}^{*}\mathbf{A}\mid\mathbf{A}$ 为合式公式 $\}$
- iii) Rule = {<A, (B*A), B > |A, B, C 均为合式公式}.

给出一个过程，确定合式公式A是否为 $\mathcal{R}$ 的定理，若是则构造A的证明。

25. 设 $\mathcal{S}$ 是由 $\mathcal{S}$ 增加所有形如 $\neg$ P(P是命题变元)的合式公式作为公理所得之系统。

- a) $\mathcal{S}'$ 是协调的吗？
- b) 给出一个过程, 确定一个合式公式 $\mathbf{A}$ 是否为 $\dot{\mathcal{S}}$ 的定理, 若它是则构造 $\mathbf{A}$ 的证明。

26. 设 $\mathcal{S}'$ 为命题逻辑系统, 且满足:

- i) Formula $\alpha =$ Formula:
- (ii) $\mathbf{A}\mathbf{x}\mathbf{i}\mathbf{o}\mathbf{m} = \{\mathbf{A}\rightarrow \mathbf{S}_{\mathbf{B}}^{\mathbf{P}}\mathbf{A}\mid\mathbf{P}$ 为命题变元，且A、B为合式公式）
- iii) $\mathbf{Rule} = \{\mathbf{MP}\}$

试讨论 $\pmb{\mathcal{S}}$ 的完全性和协调性。

27. 设 $Q$ 是一个命题演算系统, 仅含联结词 $\neg$ 和 $\wedge$ , 且满足: i) Rule = {若 A 且 $\neg$ A $\wedge$ B, 则 B|A, B 为合式公式}ii) $\mathbf{A}\mathbf{x}\mathbf{i}\mathbf{o}\mathbf{m} = \{\mathbf{A}\mathbf{S}_{1},\mathbf{A}\mathbf{S}_{2},\mathbf{A}\mathbf{S}_{3}\}$ ，其中

$$
\begin{array}{l} A S_{i}: \neg (A \wedge A) \wedge A \\ \mathrm {A S}_{2}: \neg \mathrm {A} \wedge \mathrm {\Omega} \mathrm {B} \wedge \mathrm {A} \\ A S_{3}: \neg (\neg A \wedge B) \wedge \bullet \neg (C \wedge A) \wedge \bullet B \wedge C \\ \end{array}
$$

证明: $Q$ 的一个合式公式为 $Q$ 的定理当且仅当它是永假式。

- 28. 设 $\varphi$ 和 $\psi$ 是 $\mathcal{P}$ 的两个指派, 证明: 若对任意合式公式 A 中的每个变元 p 均有 $\varphi(p) = \psi(p)$ , 则 $\bigvee_{\varphi}(A) = \bigvee_{\psi}(A)$
- 29. 设 $Q$ 是任意一个绝对协调的命题演算系统, 那么 $Q$ 的定理一定都是永真式吗? 证明你的结论。
- 30. 设 $\mathcal{S}'$ 是命题演算系统, 且满足:

- i) Formula $\mathbf{\rho}_{\mathcal{F}} = \mathbf{F}$ oula;
- ii) Rule $= \{\mathbf{MP}, \mathbf{sub}\}$ ;
- ii) $\mathbf{A}\mathbf{x}\mathbf{i}\mathbf{o}\mathbf{m} = \{\mathbf{A}\mathbf{S}_{1},\mathbf{A}\mathbf{S}_{2},\mathbf{A}\mathbf{S}_{3}\}$ ，其中

$$
\begin{array}{l} \mathbf {A S}_{1}: \mathbf {p} \vee \mathbf {p} \rightarrow \mathbf {p} \\ \mathbf {A S}_{2}: \mathbf {p} \rightarrow = \mathbf {\Omega} \mathbf {q} \vee \mathbf {p} \\ \end{array}
$$

$\mathbf{AS}_3;\mathfrak{p}\rightarrow \mathfrak{q}\rightarrow \mathbf{\Omega}_{\bullet}\mathbf{r}\vee \mathfrak{p}\rightarrow \mathbf{\Omega}_{\bullet}\mathbf{q}\vee \mathbf{r}(\mathfrak{p},\mathbf{q},\mathbf{r}$ 为命题变元）证明：a) $\mathcal{F}'$ 与 $\mathcal{P}$ 有相同的定理；

b) $\mathcal{P}'$ 推导规则的独立性。

31. 设 $\mathcal{P}$ 是一个命题演算系统, 且满足:

- i) Formula $\cdot =$ Formula;
- ii) Rule $= \{\mathbf{MP}, \mathbf{sub}\}$ ;
- iii) $\mathbf{A}\mathbf{x}\mathbf{i}\mathbf{o}\mathbf{m} = \{\mathbf{A}\mathbf{S}_{\mathrm{i}},\mathbf{A}\mathbf{S}_{\mathrm{j}},\mathbf{A}\mathbf{S}_{\mathrm{k}}\}$

其中：ASpVp>p

$$
\begin{array}{l} A S_{j}: p \rightarrow_ {\mathbf {q}} q \vee p \\ A S_{k}: p \rightarrow q \rightarrow_ {\square} r \vee p \rightarrow_ {\square} q \vee r \\ \end{array}
$$

- (1) 证明: $\mathcal{G}'$ 与 $\mathcal{G}$ 有相同的定理;
- (2) 证明: $\mathcal{P}'$ 的推演规则的独立性。

32. 证明：若 $\mathcal{P}$ 系统的三个公理模式中的任何一个被替换成模式 $\neg \mathrm{A} \vee \mathrm{A}$ 或 $\mathrm{A} \vee \neg \mathrm{A}$ ，则所得之系统不再是完全的。

33. 设 $\mathcal{G}^*$ 为一个命题演算系统，其联结词为 $\neg$ 和 $\rightarrow$ ，且满足：

- i) Rule $=\{\mathbf{MP}\}$
- ii) $\mathbf{A}\mathbf{x}\mathbf{i}\mathbf{o}\mathbf{m} = \{\mathbf{A}\mathbf{S}_{1},\mathbf{A}\mathbf{S}_{2},\mathbf{A}\mathbf{S}_{3}\}$ 其中

$$
\begin{array}{l} A S_{i}: A \rightarrow_ {i} B \rightarrow A; \\ A S_{2}: A \rightarrow (B \rightarrow C) \rightarrow_ {\square} A \rightarrow B \rightarrow_ {\square} A \rightarrow C \\ A S_{3}: \neg \neg A \rightarrow A \\ \end{array}
$$

证明：从 $\mathcal{P}^{\bullet}$ 不能导出 $\mathrm{P} \rightarrow \neg \neg \mathrm{P}$ .[提示：考虑二值解释，其中 $\rightarrow$ 与通常解释相同，而～不解释为否定].

- 34. 证明: $\{\neg, \equiv\}$ 不是完全集
- 35. 证明：每个合式公式都有合取范式。
- 36. $\{\equiv ,\rightarrow ,\vee ,\wedge \}$ 是完全集吗？证明你的结论.
- 37. 证明：仅含联结词“ $\equiv$ ”的命题合式公式是永真式当且仅当每个命题变元都仅出现偶数次。
- 38. 证明：能单独构成完全集的二元联结词只有“|”和“ $\downarrow$ ”。
- 39. 若 $\mathbf{A}$ 是一个仅含联结词“ $\equiv$ ”的合式公式， $\varphi$ 是一个指派，证明： $V_{\varphi}(A) = \text{false}$ 当且仅当满足 $\varphi(p) = \text{false}$ 的命题变元在 $\mathbf{A}$ 中恰出现奇数次。
- 40. 在仅含 $\wedge$ ， $\neq$ ，t 和 f 为联结词的合式公式中，可以将 $\wedge$ ， $\neq$ ，t 和 f 分别写成 $x, +, 1$ 和 0，采用这种写法可得如下永真式：

- a) $(\mathbf{P} + \mathbf{Q}) + \mathbf{R}) \equiv \mathbf{\Pi} \mathbf{P} + (\mathbf{Q} + \mathbf{R})$
- b) $(\mathbf{P} \times \mathbf{Q}) \times \mathbf{R}) \equiv \mathbf{P} \times (\mathbf{Q} \times \mathbf{R})$
- c) $(\mathbf{P} + \mathbf{Q}) \equiv \mathbf{\Phi}_{\mathbf{Q}} + \mathbf{P}$ ;
- d) $(\mathbf{P} \times \mathbf{Q}) \equiv_{\bullet} \mathbf{Q} \times \mathbf{P}$ ;
- e) $(\mathsf{P} + \mathsf{Q})\times \mathsf{R}\equiv_{\bullet}(\mathsf{P}\times \mathsf{R}) + (\mathsf{Q}\times \mathsf{R})$
- f $(\mathbf{P} + \mathbf{P})\equiv 0;$
- g) $(\mathbf{P}\times \mathbf{P})\equiv \mathbf{P};$

-230-

- h) $(\mathbf{P} + \mathbf{O})\equiv \mathbf{P}$
- i) $(\mathbf{P}\times \mathbf{O})\equiv \mathbf{O};$
- j) $(\mathbf{P}\times 1)\equiv \mathbf{P}.$

对应于每个形如 $(\mathbf{D} \equiv \mathbf{E})$ 的模式，我们可以得到一条推演规则：用 $\mathbf{E}$ 替换一个合式公式 $\mathbf{D}$ 中的任何出现，通过使用 $a) \neg j)$ 可以把任何一个合式公式变换成一个多项式。

证明：一个仅含 $\wedge$ ， $\neq$ ， $t$ 和 $f$ 的合式公式 $A$ 是永真式当且仅当存在一个把 $A$ 变换到 1 的规则应用序列。

- 41. 证明：若A是一个否定范式，则在C(A)中所有合式公式的析取是A的一个析取范式。
- 42. 给出一个等价于 $(p \rightarroweq q)$ 的合式公式，其中只含联结词“|”。
- 43. 一个二元联结词“*”是可结合的当且仅当 $(p*q) *r \equiv p * q * r$ 是永真式。试问：“|”是可结合的吗？为什么？
- 44. 设 $Q$ 是一个仅含联结词“|”的命题演算系统，其公理和推演规则如下：

$$
\text {A x i o m :} \mathbf {p} | (\mathbf {p} | \mathbf {r}) | \bullet \mathbf {p} | (\mathbf {r} | \mathbf {p}) | \bullet s | \mathbf {q} | \bullet \mathbf {p} | s \mid p | s
$$

Rule: i) 若 A, 则 $S_{B}^{P}A$ ;ii) 若 $A\mid_{\square} B\midC$ 且 $A$ , 则 $C$ ;证明：Q是协调的。

45. 一个命题演算的合式公式集 $S$ 是析取有效的当且仅当对任何指派 $\varphi$ ，存在 $A \in S$ 使 $\mid = _{\varphi}A$ 。证明：一个合式公式集 $S$ 是析取有效的当且仅当 $S$ 的某些有限子集是析取有效的。

用演绎法证明第 $46\neg 52$ 题所给的结论：

- 46. $\neg A\vert -A\rightarrow B.$
- 47. $A \rightarrow B, \neg (B \rightarrow C) \rightarrow \neg A \mid - A \rightarrow C$ .
- 48. $\mathbf{A} \rightarrow \neg (\mathbf{B} \rightarrow \mathbf{B})\vert -\neg \mathbf{A}$
- 49. $(\mathbf{A} \vee \mathbf{B}) \rightarrow \mathbf{C} \vdash (\mathbf{A} \rightarrow \mathbf{C}) \land (\mathbf{B} \rightarrow \mathbf{C})$
- 50. $(\mathbf{A}\wedge \mathbf{B})\rightarrow \mathbf{C}\vdash (\mathbf{A}\rightarrow \mathbf{C})\lor$ $(\mathbf{B}\rightarrow \mathbf{C})$
- 51. $\mathbf{A} \rightarrow (\mathbf{B} \vee \mathbf{C}) \vdash (\mathbf{A} \rightarrow \mathbf{B}) \vee (\mathbf{A} \rightarrow \mathbf{C})$ .

52. $\mathbf{A} \rightarrow (\mathbf{B} \wedge \mathbf{C}) \vdash \vdash (\mathbf{A} \rightarrow \mathbf{B}) \wedge (\mathbf{A} \rightarrow \mathbf{C})$ .给出第 $27\neg 30$ 题所给定理的证明序列：

53. $(\mathbf{A}\equiv \neg \mathbf{B})\rightarrow \neg (\mathbf{A}\equiv \mathbf{B})$54. $\mathbf{A} \rightarrow (\mathbf{B} \rightarrow \mathbf{C}) \equiv (\mathbf{A} \wedge \mathbf{B} \rightarrow \mathbf{C})$ .55. $\mathbf{A} \wedge \mathbf{B} \rightarrow \mathbf{C} \equiv (\mathbf{A} \rightarrow \mathbf{C}) \vee (\mathbf{B} \rightarrow \mathbf{C})$ .56. $\mathbf{A} \rightarrow \mathbf{B} \vee \mathbf{C} \equiv (\mathbf{A} \rightarrow \mathbf{B}) \vee (\mathbf{A} \rightarrow \mathbf{C})$ .57. 证明: $\{\equiv, \vee\}$ 不是完全集。

58. 证明：命题联结词 $\neg$ ， $\vee$ ， $\wedge$ ， $\rightarrow$ ， $\equiv$ 都可用“|”或“ $\downarrow$ ”来表示。

59. 设 $\mathbf{A}$ 是命题演算中的合式公式， $\mathbf{A}$ 中有不同的命题变元 $\mathbf{x}_1, \mathbf{x}_2, \dots, \mathbf{x}_n$ 把 $\mathbf{A}$ 变换为满足以下 (1) $\neg$ (5) 的 $\mathbf{B}$ ：

- (1) $B = C_{1} \vee C_{2} \vee \dots \vee C_{m}$ ;
- (2) $C_{i} = C_{i1}\wedge \dots \wedge C_{in}(i = 1,2,\dots ,m)$
- (3) 每个 $C_{k}$ 是 $\mathbf{x}_{\mathbf{k}}$ 或 $\neg x_{k}(i = 1,2,\dots ,m,k = 1,2,\dots ,n)$
- (4) $C_1, \cdots, C_m$ 互异，且按照这样的规则来排序，即如果 $C_{i1}, \cdots, C_{i(k-1)}$ 分别与 $C_{j1}, \cdots, C_{j(k-1)}$ 相同（ $i = 1, \cdots, m; j = 1, \cdots, m$ ），而 $C_{ik} = x_k, C_{jk} = -x_k$ ，则 $i < j$ ；
- (5) A 和 B 有相同的真值表.

则称 $\mathbf{B}$ 为A的完全析取范式.证明：

- a) 如果 $\mathbf{A}$ 不是永假式, 则 $\mathbf{A}$ 有唯一的完全析取范式.
- b) A是永真式当且仅当在A的完全析取范式B中 $m = 2^{n}$

60. 求下列公式的合取范式和析取范式：

- a) $\mathbf{p} \equiv \neg (\mathbf{q} \wedge \mathbf{r})$
- b) $(\mathbf{p} \vee \mathbf{r}) \equiv (\mathbf{q} \wedge \neg \mathbf{p})$
- c) $(\mathbf{q} \vee \mathbf{r}) \rightarrow \mathbf{p}$

61. 对下列合式公式分别找出其等价合式公式（仍用联结词～，∧， $\vee$ ， $\rightarrow$ 和 $\equiv$ ），并使其中出现的命题变元尽可能少。

a) $\mathbf{p} \rightarroweq \mathbf{q} \rightarrow \mathbf{p}$

- b) $(\mathbf{p} \wedge \mathbf{q}) \vee (\mathbf{p} \wedge \mathbf{r})$
- c) $\mathbf{p} \equiv \neg (\mathbf{p} \equiv \mathbf{q})$
- d) $\neg$ (\~p∧■p>q)

试判断能否由下面第 $62\neg 65$ 题所给短句集分别消解（resolution）出“口”，并证明你的结论。

62. (1) pV r(4) $\neg rV\neg s$(2) $q \vee s$(5) $q \vee r$(3) $\neg \mathbf{p} \vee \neg \mathbf{q}$(6) $\mathbf{p} \vee \mathbf{s}$63. (1) $\mathbf{p} \vee \mathbf{q}$(5) $q \vee r \vee p$(2) $\neg \mathbf{p} \vee \mathbf{r} \vee \neg \mathbf{q} \vee \mathbf{p}$(6) $r \vee q \vee \neg p \vee \neg r$(3) $\mathfrak{p} \vee \neg \mathfrak{r}$(7) $\neg r \vee q$(4) $q \vee \neg p \vee \neg q$64. (1) $q \vee r$(4) $\neg p \vee \neg q \vee \neg r$(2) $\neg \mathbf{r} \vee \mathbf{p}$(5) $p \vee q \vee r$(3) $\neg \mathbf{p} \vee \mathbf{q}$65. (1) $p \lor q \lor r$(4) $p \vee  \neg s$(2) $\neg q \vee s$(5) $\neg p$(3) $\neg \mathbf{r} \vee \mathbf{s}$(6) $q \vee \neg r$用消解法证明第 $66\neg 68$ 题所给合式公式是否为永真式：

- 66. $(\mathfrak{p}\wedge \bullet \mathfrak{q}\rightarrow \mathfrak{r})\rightarrow \bullet (\mathfrak{s}\vee \mathfrak{p})\wedge \bullet \neg \mathfrak{r}\rightarrow \neg \mathfrak{q}$
- 67. $(\mathbf{p} \vee \mathbf{q}) \wedge (\neg \mathbf{p} \vee \neg \mathbf{r} \rightarrow \mathbf{s}) \wedge (\mathbf{r} \rightarrow \mathbf{s}) \wedge (\mathbf{r} \vee \neg \mathbf{q})) \rightarrow \mathbf{q} \wedge \mathbf{s}$
- 68. $(\mathfrak{p} \vee \mathfrak{q}) \wedge (\neg s \rightarrow \mathbf{p} \rightarrow \mathbf{r}) \wedge ((\mathbf{r} \vee \mathbf{s}) \rightarrow \mathbf{q})) \rightarrow \mathbf{q}$

证明下述合式公式为永假式：

$$
\begin{array}{l} (\neg d \vee \neg e) \wedge (s \vee q) \wedge (h \vee k \wedge (m \vee e \vee a) \wedge (p \vee j \vee \\ b) \bigwedge ((\neg s \wedge \neg q) \vee \square \neg h \wedge (\neg r \vee \neg a) \wedge (r \vee a)) \wedge \\ (\neg \mathrm {r} \vee \text {口} \neg \mathrm {m} \wedge \text {口} \neg \mathrm {k} \vee \mathrm {d} \vee \mathrm {a}) \wedge (\mathrm {r} \vee \mathrm {m} \vee \text {口} \neg \mathrm {p} \wedge \mathrm {g} \wedge \neg \\ \mathrm {c}) \wedge (\neg \mathrm {a} \vee \mathrm {n} (\neg \mathrm {k} \vee \neg \mathrm {m}) \wedge (\neg \mathrm {b} \vee \mathrm {c}) \wedge (\neg \mathrm {j} \vee \neg \mathrm {g})) \\ \end{array}
$$

70. 给定合式公式如下：

- a) $\neg ((\forall uP(u,z)\wedge \neg \exists x_{\parallel}Q(u,x)\vee \forall z\neg R(x,z))\rightarroweq \exists zQ(u,z)$
- b) $\forall x(P(u, x) \vee \exists y Q(x, y) \wedge \neg \forall u R(u, z) \rightarroweq \forall z Q(z, u)$

试确定：

- (1) 合式公式中变元的自由出现;
- (2）合式公式中所有自由变元；
- (3）合式公式中所有约束变元；
- (4) $\mathbf{g}(\mathbf{x})$ 对合式公式中的 $\mathfrak{u}$ 是否为自由的；
- (5) $h(x, y)$ 对合式公式中的 $u$ 是否为自由的；
- (6) $u$ 对合式公式中的 $x$ 是否为自由的。

71. 设 $\mathbf{H}(\mathbf{x})$ 表示: $\mathbf{x}$ 是一匹马;$\mathbf{t}(\mathbf{x})$ 表示： $\mathbf{x}$ 的尾巴；

$\mathbf{m}(\mathbf{x})$ 表示： $\mathbf{x}$ 的鬃毛；

$\mathbf{B}(\mathbf{x})$ 表示： $\mathbf{x}$ 在牲口棚；

W(x)表示：x是白色的；

$\mathbf{K}(\mathbf{x})$ 表示： $\mathbf{x}$ 是黑色的；

L(x,y)表示：x象y.试将下列汉语命题 $(\mathcal{F}$ 的合式公式)翻译成 $\mathcal{F}$ 的合式公式(汉语命题):

- a）在牲口棚中的每一匹白尾巴马都有黑色的鬃毛；
- b）白马不象有黑色鬃毛的马；
- c）在牲口棚中没有白尾巴马；
- d) $(\exists x \in H)(B(x) \wedge (\forall y \in H) B(y) \wedge K(t(y)) \rightarrow L(x, y))$
- (e) $\neg$ (3 $x\in H)$ $(\mathbf{B}(\mathbf{x})\wedge \neg \mathbf{W}(t(\mathbf{x})));$

72. 设 $\mathbf{B}(\mathbf{x},\mathbf{y})$ 表示： $\mathbf{x}$ 是 $\mathbf{y}$ 的兄弟；

r表示：Robert;$\mathbf{U}(\mathbf{x}, \mathbf{y})$ 表示： $\mathbf{x}$ 是 $\mathbf{y}$ 的叔叔；

$\mathbf{f}(\mathbf{x})$ 表示： $\mathbf{x}$ 的父亲；

$\mathbf{C}(\mathbf{x}, \mathbf{y})$ 表示: $\mathbf{x}$ 是 $\mathbf{y}$ 的堂兄弟;$\mathbf{Y}(\mathbf{x}, \mathbf{y})$ 表示: $\mathbf{x}$ 比 $\mathbf{y}$ 年轻;试将下列汉语命题 $(\mathcal{F}$ 的合式公式)翻译成 $\mathcal{F}$ 的合式公式(汉语命题):

- a) Robert 父亲的每个兄弟都是 Robert 的叔叔;
- b) $\forall x\forall y(B(f(x),f(y))\rightarrow C(x,y))$
- c) Robert 有一个堂兄弟比他的某个兄弟年轻。

73. 指出下列公式中的自由变元与约束变元，并说明什么样的项 $t$ 对这些自由变元是可代入的：

- (1) $\exists xP(x)\land P(x)$
- (2) $\forall x(P(x) \land Q(y) \rightarrow \exists y (\neg S(y) \land R(x))$ .

- 74. 设由公式集 $\Gamma$ 导出公式A的演绎序列长度为 $n$ , $x$ 在 $\Gamma$ 中无自由出现, 试求函数 $f$ , 使 $f(n)$ 为由 $\Gamma$ 导出 $\forall x A$ 的演绎序列的长度。
- 75. 设 $A, C$ 为合式公式， $m$ 为命题变元， $C$ 对 $A$ 中的 $m$ 是自由的，对 $C$ 中的自由变元 $x_1, x_2, \cdots, x_n$ 以及在 $C$ 和 $A$ 中不出现的不同个体变元 $u_1, u_2, \cdots, u_n$ ，证明： $S_{x_1 \cdots x_n}^{u_1 \cdots u_n} S_B^m A = S_c^m A$ ，其中 $B = S_{u_1 \cdots u_n}^{x_1 \cdots x_n} C$ .
- 76. 证明: 若 $(\mathbf{B} \vee \mathbf{C})$ 和 $(\mathbf{D} \vee \mathbf{E})$ 是相同的合式公式,

则 $\mathbf{B} = \mathbf{D}$ 且 $C = E$

- 77. 若 $x, y$ 是不同的个体变元，且 $y$ 在 $c$ 中不自由， $y$ 对 $c$ 中的 $x$ 是自由的，则 $\mathbf{S}_x^y \mathbf{S}_y^x \mathbf{C} = \mathbf{C}$
- 78. 从 $\forall x(G(x) \rightarrow H(x)) \equiv \exists xH(x)$ 和 $\forall x(G(x) \rightarrow H(x)) \equiv \neg \neg \forall xG(x)$ $\rightarrow \exists xH(x)$ 通过 P 规则可推出 $\exists xH(x) \rightarrow \neg \forall xG(x)$ 吗？为什么？

在 $79\neg 86$ 题中证明所给合式公式为永真式：

- 79. $\forall x(R(x) \rightarrow P(x)) \land \forall x(\neg Q(x) \rightarrow R(x)) \rightarrow \forall x(P(x) \lor Q(x))$
- 80. $\forall x\exists y(P(x)\rightarrow P(y))$

- 81. $\exists x(P \land Q(x)) \equiv (P \land \exists xQ(x))$
- 82. $\exists y\forall x(P(y)\rightarrow P(x))$
- 83. $\exists x(P(x) \rightarrow Q(x)) \equiv (\forall x P(x) \rightarrow \exists x Q(x))$
- 84. $\exists x\forall y(P(x)\equiv P(y))\equiv (\exists xP(x)\equiv \forall yP(y))$
- 85. $\forall x(P(x) \equiv \exists yP(y)) \equiv (\forall xP(x) \equiv \exists yP(y))$
- 86. $\exists x\forall y(P(y)\equiv P(x))\rightarrow (\forall xP(x)\lor \forall x\neg P(x))$
- 87. 证明：

- a) $\vdash (\exists x \in S) P(x) \equiv \neg (\forall x \in S) \neg P(x)$
- b) $\vdash (\forall x\in S)P(x)\equiv \neg$ （20 $(\exists x\in S)\neg P(x)$

88. 求下列矫正合式公式的前束范式：

- a) $\neg \forall x R(x, u) \land \exists y (\neg \forall z R(y, z) \rightarrow Q(u, y))$
- b) $\neg$ ( $\exists xQ(x,y)\rightarrow \neg \forall z(\forall uP(u,y,z)\rightarrow \neg \exists vR(v))$ ）

89. 求下列合式公式的前束范式：

- a) $\forall xP(x)\equiv \exists yQ(y)$
- b) $\forall x\neg \exists y\forall uP(x,y)\rightarrow \neg$ （204 $(\exists zP(x,y)\rightarrow \forall yP(u,y))$

- 90. 证明： $\models_{(\mathcal{I}, \sigma)} \exists xA$ 当且仅当存在指派 $\varphi$ ，使得 $\varphi(y) = \sigma(y) (x \neq y)$ ，且 $\models_{(\mathcal{I}, \varphi)} A$ 。
- 91. 证明：

- a) $\vdash \exists x \forall y \exists z(P(z, x) \land P(x, y) \rightarrow P(y, y))$
- b) $\vdash \exists x\forall y(Q(x)\rightarrow (P(x)\lor Q(y))$

- 92. 找到一个合式公式A使 $(\mathsf{A}\rightarrow \forall \mathsf{x}\mathsf{A})$ 为有效的。
- 93. 找到一个合式公式B使 $(\mathbf{B} \rightarrow \forall \mathbf{x}\mathbf{B})$ 不是有效的。
- 94. 证明：若 $C$ 是任意合式公式， $I$ 是任意解释，则 $(C \rightarroweq \forall x C)$ 在 $I$ 下是可满足的。
- 95. 对 $\mathcal{F}$ 的每个合式公式A而言， $\models A$ 当且仅当 $\models A \rightarrow \forall x A$ 成立吗？
- 96. 设 $A(x)$ 是合式公式 $P(x) \rightarrow P(a) \land P(b)$ ，证明：

$\vDash$ $\exists xA(x)$ ，但不存在项t使得 $\mid = A(t)$97. 判断下列形式的合式公式是否为有效的，可满足的？

- a) $\exists x(M \equiv N) \rightarrow (\exists xM \equiv \exists xN)$
- b) $\forall x(M\equiv N)\rightarrow (\forall xM\equiv \forall xN)$

98. 设A, B, C为合式公式，且定义如下：

$$
\begin{array}{l} A = \exists z (\exists x \forall y P (x, y, z) \rightarrow \exists w Q (x, z) \\ B = \exists z (\forall y \exists x P (x, y, z) \rightarrow \exists w Q (w, z) \\ \end{array}
$$

$$
C = \forall z \forall y \exists x P (x, y, z) \rightarrow \exists z \exists w Q (w, z)
$$

对任意两个不同的合式公式D，E∈{A，B,C}，判断D>E是否为有效的？

在 $99\neg 103$ 题中，确定所给合式公式是否为有效的，是则给出证明，否则给出其反模型：

- 99. $(\exists xP(x) \rightarroweq \forall xQ(x) \rightarroweq \forall x(P(x) \rightarroweq Q(x))$
- 100. $\forall x(P(x) \rightarrow Q(x)) \rightarrow (\exists xP(x) \rightarrow \forall xQ(x))$
- 101. $\forall x(P(x) \rightarrow Q(x)) \equiv (\forall x P(x) \rightarrow \exists x Q(x))$
- 102. $\exists x\forall y(P(x)\equiv P(y))$
- 103. $\exists x\forall yR(x,y)\rightarroweq \forall x\exists yR(x,y)$
- 104. 设一阶逻辑系统的常元如下：

个体常元：1

- 二元个体函数常元：+
- 一元谓词常元：E,O
- 二元谓词常元： $<, =$

今取解释 $\mathcal{I} = <  \mathrm{N},\mathcal{I}_0 > ,\mathcal{I}' = <  \mathrm{R},\mathcal{I}_0'$ 如下：

- ① 论域：N为自然数集，R为实数集；
- ② 常元取通常意义：

“1”为自然数1；“+”为加$\mathbf{E}(\mathbf{x})$ ： $\mathbf{x}$ 是偶数； $\mathbf{O}(\mathbf{x})$ ： $\mathbf{x}$ 是奇数；

“<”为小于关系，“=”为等于关系。

证明：

- (1) $\forall x \forall y (x + y = 1)$ 在 $\mathcal{I}$ 和 $\mathcal{I}'$ 下均为假；
- (2) $\forall x \exists y (x + y = 1)$ 在 $\mathcal{I}$ 为假, 在 $\mathcal{I}$ 下为假;
- (3) $\exists x \forall y (x + y = 1)$ 在 $s$ 和 $f'$ 下均为假；
- (4) $\forall x \exists y (x + y = 1) \rightarrow \exists x \forall y (x + y = 1)$ 在 $\mathcal{I}$ 下为真，在 $\mathcal{I}'$ 下为假；
- (5) $\exists xO(x) \rightarrow \forall xO(x)$ 在 $\mathcal{I}$ 下为假。

- 105. 判断是否存在形如 $\forall x(A \lor B) \rightarrow A \lor \forall xB$ 的有效合式公式（其中 $x$ 在 $A$ 中是自由的）？
- 106. 设 A, B 为可满足的句子, 判断以 $\{A, B\}$ 为公设集的一阶理论是否为协调的? 为什么?
- 107. 设 M, N, C 和 C 是 $\mathcal{F}$ 的合式公式，其中 C 是用 N 替换 M 在 C 中的一次肯定出现所得之结果且 $\mid-M \lor N$ ，试问 $\mid-C \lor C'$ 是否成立？为什么？
- 108. 设 $M, N, C$ 和 $C'$ 是 $\mathcal{F}$ 的合式公式，其中 $C'$ 是用 $N$ 替换 $M$ 在 $C$ 中一次肯定出现所得之结果，且 $\mid -M \wedge N$ ，试问 $\mid -C \wedge C'$ 是否成立？为什么？
- 109. 证明：不存在 $\mathcal{F}^{\infty}$ 的句子构成的协调集S，使得S的所有模型都是等词模型。
- 110. 对 $\mathcal{F}$ 的任何合式公式A，设 $A^{\prime}$ 如下定义：

i) 若 $\mathbf{A}$ 为原子，则 $\mathbf{A}' = \mathbf{A}$ii) $(\neg \mathbf{B})' = \neg \mathbf{B}; (\mathbf{B} \vee \mathbf{C})' = \mathbf{B}' \vee \mathbf{C}';$

$$
(\forall \mathbf {x} \mathbf {B})^{\prime} = \exists \mathbf {x} \mathbf {B}^{\prime}
$$

则对任意 $\mathsf{A}\in \mathcal{L}(\mathcal{F}),\vdash -\mathsf{A}\rightarrow \mathsf{A}'$ 成立吗？

- 111. 设 $\Gamma \subseteq \mathcal{L}(\mathcal{F})$ ，证明：若 $\Gamma$ 为协调的，则必有 $\Gamma' \subseteq \mathcal{L}(\mathcal{F})$ 使 $\Gamma'$ 既是完全的又是协调的且 $\Gamma \subseteq \Gamma'$ .
- 112. 设 $\mathbf{H}$ 是 $\mathcal{F}$ 的句子集，若对任何 $\mathbf{F}$ 的句子 $\mathbf{A}$ 必有 $\mathrm{H}\mid- \mathrm{A}$ 或 $\mathrm{H}\mid- \neg \mathrm{A}$ ，则称 $\mathbf{H}$ 是完全的。证明：若 $\mathbf{G}$ 是任何协调的句子集，则存在一个完全的、协调的句子集 $\mathbf{H}$ 使 $\mathbf{G} \subseteq \mathbf{H}$ 。
- 113. 若对任何解释 $\mathcal{I}$ 和指派 $\sigma$ 满足：如果 $\mid = _{(s,\sigma)}A$ ，则 $\mid = _{(s,\sigma)}B$ ，则称 $\mathbf{B}$

-238-是A的（语义）结果，考查下列断言是否成立，是则证明，否则举反例：

- (1). 若B是A的（语义）结果，则B在A的所有模型中是有效的。
- (2) 若 B 在 A 的所有模型是有效的, 则 B 是 A 的 (语义) 结果.

114. 若 $y \neq x$ ，且 $y$ 不在 $A$ 中出现，证明：

$$
\vdash \exists ! x A \equiv \exists y \forall x (A \equiv x = y)
$$

115. 若 $y \neq x$ ，且 $y$ 不在 $A$ 中出现，证明：

$$
\vdash \exists ! x A \equiv \exists x (A \wedge \forall y (S_{y}^{x} A \rightarrow x = y))
$$

116. 证明：

$$
\vdash \forall x \forall y (x = y) \rightarrow (\exists x P (x) \equiv \forall x P (x))
$$

117. 设 $=$ 不是 $\mathcal{F}$ 中的谓词符号，证明：

$\mathcal{F}$ 是 $\mathcal{F}$ 的一个保守扩张。

118. 若 $y \neq x$ 且 $y$ 不在 $A$ 中出现，证明：

$$
\vdash \exists ! x A \equiv \exists x A \wedge \exists !! x A
$$

## 用演绎法证明第 $119\neg 121$ 题所给的结论：

- 119. $\vdash \exists ! x P(x) \equiv \exists x \square P(x) \land V y \square P(y) \rightarrow x = y.$
- 120. $\vdash \exists ! x P(x) \equiv \exists y \forall x P(x) \equiv x = y$
- 121. $\exists ! x P(x) \mid - \mid \exists x \forall y (P(y) \equiv x = y)$

## 用演绎法证明第 $122\neg 127$ 题所给的结论：

- 122. $\vdash -\forall x(A \rightarrow B) \equiv (\exists x A \rightarrow B), x$ 在 $B$ 中不自由。
- 123. $\vdash -\neg (\neg S_x^z A \vee S_y^z A) \rightarrow x \neq y, A$ 为原子公式.
- 124. $\vdash \exists x(A \rightarrow B) \equiv (\forall x A \rightarrow B)$ , $x$ 在 $B$ 中不自由.
- 125. $\forall x(A(x) \rightarrow B(x)), \forall x A(x) \vdash -\forall x B(x)$ .
- 126. $\mathbf{A} \rightarroweq \forall x (\mathbf{B}(x)) \mid- \mid\forall x (\mathbf{A} \rightarrow \mathbf{B}(x)), x$ 在 A 中不出现。
- 127. $\forall x(A(x) \rightarrow B(x)), \exists x(A(x)) \vdash \exists xB(x)$ .

128. 设 $\Gamma \subseteq$ Formula, A∈Formula, 且 $\mathcal{I} = <\mathcal{D}, \mathcal{I}_0>$ 为 $\mathcal{F}$ 的一个解释: 证明若 $\Gamma \models A$ , 则对每个 $\Gamma$ 的模型 $\mathcal{I}'$ 均有 $\mid=, A$ , 反之成立吗? 为什么?129. 给出句子 $\forall x(P(x)) \equiv \neg P(f(x))$ 的一个模型，(其中 $p$ 和 $f$ 都是常量).130. 对于下列各种情况，分别给出合式公式集 $\Gamma$ ，使得 $<\mathcal{D}, \mathcal{I}_0>$ 为 $\Gamma$ 的等词模型当且仅当：

- i) $\mathcal{D}$ 中恰好含有一个元素；
- ii) $\mathcal{D}$ 中恰好含有两个元素；
- iii) $\mathcal{D}$ 为无穷集合。

131. 设 $\mathbf{P}$ 为一元谓词，对于下列各种情况，分别给出合式公式集 $\Gamma$ ，使得 $<\mathcal{D}, \mathcal{I}_0>$ 为 $\Gamma$ 的等词模型当且仅当：

- i) $\mathcal{D}$ 中恰有唯一元素满足 $\mathcal{I}_0(\mathbf{P})$
- ii) $\mathcal{D}$ 中至少有 $\mathfrak{n}(\geqslant 2)$ 个元素满足 $\mathcal{I}_0(\mathbb{P})$
- iii) $\mathcal{D}$ 中至多有 $\mathfrak{n}(\geqslant 2)$ 个元素满足 $\mathcal{I}_0(\mathbb{P})$
- iv) $\mathcal{D}$ 中恰有 $\mathbf{n}(\geqslant 2)$ 个元素满足 $\mathcal{I}_0(\mathbf{P})$

- 132. 证明一阶逻辑无法表达有穷集合的概念，即不存在一阶逻辑的合式公式集 $\Gamma$ ，使得 $<\mathcal{D}, \mathcal{I}_0>$ 为 $\Gamma$ 的等词模型当且仅当 $\mathcal{D}$ 为有穷集合。
- 133. 证明无穷集合的概念无法在一阶逻辑中有穷地表达，即不存在一阶逻辑合式公式的有穷集 $\Gamma$ ，使得 $<\mathcal{D}, \mathcal{I}_0>$ 为 $\Gamma$ 的等词模型当且仅当 $\mathcal{D}$ 为无穷集合。
- 134. 给出一个表达半序（全序）关系的合式公式集 $\Gamma$ ，即 $<\mathcal{G}, \mathcal{I}_0>$ 是 $\Gamma$ 的模型当且仅当 $\mathcal{I}_0(\mathbb{R})$ 是 $\mathcal{D}$ 上的半序（全序）关系（其中 $\mathbb{R}$ 为二元谓词常元）。
- 135. 证明一阶逻辑不能表达良基（良序）关系即不存在一阶逻辑的合式公式集 $\Gamma$ ，使得 $<\mathcal{D}, \mathcal{I}_0>$ 是 $\Gamma$ 的模型当且仅当 $\mathcal{I}_0(\mathbb{R})$ 是 $\mathcal{D}$ 上的良基（良序）关系（其中 $\mathbb{R}$ 为二元谓词常元）。
- 136. 设 $\mathrm{T}$ 是一阶理论，对任意自然数 $n$ ， $\mathrm{T}$ 有基数大于 $n$ 的模型。证明 $\mathrm{T}$

有无穷模型。

- 137. 设 $\Gamma, \Gamma' \subseteq \mathcal{L}(\mathcal{S})$ 且 $\Gamma$ 与 $\Gamma'$ 不协调。证明：必有一个合式公式 A 使得 $\Gamma - A$ 且 $\Gamma' - A$ .
- 138. 证明：若解释 $\mathcal{I} = < \mathcal{D},\mathcal{I}_0>$ 满足合式公式：

$\forall x P(x, x) \land \forall x \forall y \forall z (P(x, y) \land P(y, z) \rightarrow P(x, z)) \land \forall x \forall y (P(x, y) \lor P(y, x)) \rightarrow \exists y \forall x P(y, x)$ ，则论域 $\mathcal{D}$ 必为无穷集合（ $P$ 为二元谓词常元）。

## 用自然演绎系统 $\mathcal{N}$ 证明下述定理：

- 139. $\vdash -\neg \forall x A \equiv \exists x \neg A.$
- 140. $\vdash -\neg \exists xA\equiv \forall x\neg A.$
- 141. $\mid -\forall u\forall v\forall w(P(u,v)\lor P(v,w))\rightarrow \exists x\forall yP(x,y).$
- 142. $\vdash \exists x(A(x) \land B(x)) \equiv \exists xA(x) \lor \exists xB(x)$ .
- 143. $\mid -\forall x(A(x)\wedge B(x))\equiv \forall xA(x)\wedge \forall xB(x).$
- 144. $\vdash -\exists x(A(x)\land B(x))\rightarrow \exists xA(x)\land \exists xB(x).$
- 145. $\mid -\forall xA(x)\vee \forall xB(x)\rightarrow \forall x(A(x)\vee B(x))$
- 146. $\vdash -\exists x\forall yA(x,y)\rightarrow \forall y\exists xA(x,y).$
- 147. $\mid -\forall x(A(x)\rightarrow B(x))\rightarrow \square \forall xA(x)\rightarrow \forall xB(x)).$
- 148. $\mid -\forall x\exists y(P(x)\rightarrow P(y))$
- 149. $\vdash \exists x\forall y(P(x)\rightarrow P(y))$
- 150. $\vdash -\exists x\exists y\forall u(P(x,y,z)\rightarrow P(u,x,x))$

## 利用Gentzen系统证明下述定理：

- 151. $\vdash A \vee (B \wedge C) \equiv (A \vee B) \wedge (A \vee C)$ .
- 152. $\vdash \neg (\mathbf{A} \vee \mathbf{B}) \equiv (\neg \mathbf{A} \wedge \neg \mathbf{B})$ .
- 153. $\vdash \neg (\mathbf{A} \wedge \mathbf{B}) \equiv (\neg \mathbf{A} \vee \neg \mathbf{B})$ .
- 154. $\vdash \neg \mathbf{A} \wedge (\mathbf{A} \vee \mathbf{B}) \rightarrow \mathbf{B}$ .
- 155. $\vdash -\forall x A(x) \equiv \neg \exists x \neg A(x)$ .

- 156. $\vdash \exists x A(x) \equiv \neg \forall x \neg A(x)$ .
- 157. $\vdash \exists x\forall y(P(x)\land Q(y)\rightarrow \bullet Q(x)\lor P(y)).$
- 158. $\vdash \exists y(P(y) \rightarrow Q(x)) \rightarrow \exists y \rightarrow P(y) \rightarrow Q(y)$ .
- 159. $\vdash -\exists x(P(x) \rightarrow Q(x)) \equiv \neg \forall x P(x) \rightarrow \exists x Q(x)$ .

## 求下列公式的skolem标准型：

- 160. $\exists y\forall z(P(z,y)\equiv \neg (\exists x)(P(z.x)\land P(x,z)))$
- 161. $\neg$ ( $\forall x \exists yP(a, x, y) \rightarrow \exists x (\neg \forall yQ(y, b) \rightarrow R(x))$ .
- 162. $\exists x\forall y\exists z(P(z,x)\land P(x,y)\rightarrow P(y,y))\land \exists xP(x,x).$
- 163. $\neg \exists x \exists y \forall z (\mathbf{P}(x, y) \rightarrow \mathbf{P}(y, z) \land \mathbf{P}(z, z)) \land (\mathbf{P}(x, y) \land Q(x, y) \rightarrow Q(x, y) \land Q(z, z))$ .

## 用Herbrand定理证明下列公式为永真式：

- 164. $\exists x\forall y(P(x)\rightarrow P(y))$
- 165. $\forall x\exists y(P(x)\rightarrow P(y))$
- 166. $\forall x(P(x) \rightarrow P(x)) \land \forall x (\neg Q(x) \rightarrow R(x)) \rightarrow \forall x P(x) \lor Q(x)$ .
- 167. $\forall x(P \wedge Q(x)) \vee \neg P \wedge R(x)) \rightarrow \neg \forall x Q(x) \vee \forall x R(x)$ .
- 168. $(\exists xR(x, x) \rightarrow \forall yR(y, y)) \rightarrow \exists u \forall v R(u, u) \rightarrow R(v, v)$ .
- 169. $\forall x(P(x) \equiv \forall yP(y)) \equiv \exists xP(x) \equiv \forall yP(y)$ .
- 170. $\forall xP(x) \rightarrow \exists y \in \forall x \forall z Q(x, y, z) \rightarrow \neg \forall z P(z) \wedge \neg Q(y, y, z)$ .
- 171. $\forall z\exists x(\forall yP(x,y)\lor Q(x,z))\rightarrow \forall y\exists x(P(x,y)\lor Q(x,y))$
- 172 $\forall x\forall y(P(x,y)\rightarrow P(y,x))\land \forall x\forall y\forall z(P(x,y)\land P(y,z)\rightarrow P(x,z))\land$ $\forall x\not\equiv yP(x,y)\rightarrow \forall xP(x,x)$

## 利用语义树方法证明下列公式为永真式：

- 173. $R(a, b) \wedge \forall x \forall y (R(x, y) \rightarrow R(y, x) \wedge Q(x, y)) \wedge \forall u \forall v (Q(u, v) \rightarrow Q(u, u)) \rightarrow Q(a, a) \wedge Q(b, b)$ .
- 174. $\exists xR(x)\land \forall y(R(y)\rightarrow \exists zQ(y,z))\land \forall x\forall y(Q(x,y)\rightarrow Q(x,x))\rightarrow$ $\exists x\exists y\equiv Q(x,y)\land R(y).$

-242-

- 175. $\forall x (\exists x \neq x Q(x, y)) \Rightarrow \forall z \exists y P(z, y) \rightarrow \forall y \forall x Q(x, y)$ .
- 176. $\forall y\exists wP(x) = \exists z / x(P(x)\rightarrow R(z,x))\rightarrow \exists x\neg P(x).$
- 177. $\forall x R(x, b) \land \exists z (z) \rightarrow R(a, y)) \rightarrow \exists u V u R(u, v)$ .
- 178. $\forall \mathbf{v} \forall \mathbf{u} (\mathbf{R}(\mathbf{u}, \mathbf{u}) \equiv \mathbf{R}(\mathbf{u}, \mathbf{v})) \land \forall \mathbf{w} \forall \mathbf{z} (\mathbf{R}(\mathbf{w}, \mathbf{w}) \equiv \mathbf{R}(\mathbf{z}, \mathbf{w})) \rightarrow \exists \mathbf{x} \mathbf{R}(\mathbf{x}, \mathbf{x}) \rightarrow \forall \mathbf{y} \mathbf{R}(\mathbf{y}, \mathbf{y})$ .
- 179. $(\exists xR(x, x)) \rightarrow \forall yR(y, y)) \rightarrow \exists u \forall v R(u, u) \rightarrow R(v, v)$ .
- 180. 设 $\Gamma = \{\neg \exists x \forall y (P(x) \rightarroweq Q(x) \lor P(y))\}$ ，试给出 $\Gamma$ 的一个 $\mathcal{R}$ 一反驳。
- 181. 设 $\Gamma = \{\forall z \exists x (\forall y P(x, y) \lor Q(x, z)), \neg \forall y \exists x (P(x, y) \lor Q(x, y))\}$ , 试给出 $\Gamma$ 的一个 $\mathcal{R}$ 一反驳。
- 182. 设 $\Gamma = \{\forall x (\exists yQ(x, y) \rightarrow P(x)), \forall v \exists uQ(u, v), \forall w \forall z(Q(w, z) \rightarrow Q(z, w) \vee Q(z, z), \neg \forall zP(z)\}$ . 试给出 $\Gamma$ 的一个 $\mathcal{R}$ 一反驳。
- 183. 设 $\Gamma = \{\forall x (\exists y P(x, y) \rightarrow \forall y Q(x, y)), \forall z \exists y P(z, y), \neg \forall y \forall x Q(x, y)\}$ ，试给出 $\Gamma$ 的一个 $\mathcal{R}$ 一反驳。

